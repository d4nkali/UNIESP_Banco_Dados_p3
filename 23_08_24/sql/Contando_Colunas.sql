-- Contando as colunas das Chaves Primarias das tabelas.

select count(cod_produto) from public.produtos;
select count(cod_usuario) from public.usuarios;
select count(cod_produto) from public.vendas;
